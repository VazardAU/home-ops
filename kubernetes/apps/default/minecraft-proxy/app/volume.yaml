---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minecraft-proxy-config
  namespace: default
  labels:
    app.kubernetes.io/name: &name minecraft-proxy
    app.kubernetes.io/instance: *name
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: rook-ceph-block
