---
# yaml-language-server: $schema=https://kubernetes-schemas.devbu.io/helmrelease_v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: immich-server
  namespace: default
spec:
  values:
    additionalContainers:
      immich-cli:
        name: immich-cli
        image: ghcr.io/immich-app/immich-cli
        volumeMounts:
          - name: temp
            mountPath: /tempupload
